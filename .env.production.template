# Template for .env.production — used ONLY by ./deploy.sh to sync static assets
# to the DigitalOcean Spaces CDN.
#
# Application configuration no longer lives here. The app is configured at
# container runtime; see .env.docker.example for that contract.

CDN_ENDPOINT=nyc3.digitaloceanspaces.com
CDN_BUCKET=s3://j2it
